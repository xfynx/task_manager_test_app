== README

This is a simple task manager app, created for rails features demonstration.
It has 2 user roles: admin and user.
Admin can assign tasks to any user, user can only take unassigned task to self.
There is a 3 different task states: new, started and finished.

Admin can close the task in any way, user can close or start task only if task was assigned to him.